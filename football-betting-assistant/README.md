# 足球竞彩助手

`football-betting-assistant` 是一个面向中文足球竞彩分析的 agent-first skill 包。它用于胜平负、让球胜平负、比分、大小球、总进球、串关和赛后复盘等场景。

## 使用方式

把 `football-betting-assistant/` 目录放到 Codex 或其他 agent 的 skills 目录中。用户提出足球投注分析、竞彩、胜平负、让球胜平负、比分推荐、大小球、总进球、串关、四串一或赛后复盘相关请求时，这个 skill 应自动触发。

当 agent 可以使用浏览器、搜索、本地文件、已配置的足球数据 API 或内置计算脚本时，效果最好。无法获取实时数据时，它应该只向用户索取最少必要数据，而不是编造赛程、赔率、首发、伤停或天气。

## 能做什么

- 根据自然语言请求发现和确认具体比赛。
- 在工具可用时核验当前赔率、盘口、球队近况、伤停、首发、天气和赛事背景。
- 使用透明模型：预期进球、贝叶斯修正、泊松比分概率和赔率隐含概率对比。
- 输出单场分析、组合分析、比分覆盖、参考购买方案和赛后复盘。

## 赔率获取途径

这个 skill 本身不内置实时赔率库，也不会声称自己天然拥有当前盘口。它依赖 agent 当前环境中可用的数据能力，并按下面优先级获取或确认赔率：

1. 用户提供的赔率、截图、表格、链接或结构化数据。
2. 用户或运行环境已经配置好的足球数据服务/API，例如赛程、赔率、首发、伤停、球队数据或天气服务。
3. 公开网页、搜索或浏览器核验到的公开/授权数据。
4. 当前数据无法确认时，向用户追问最少必要信息。

做赔率价值判断时，优先使用多来源核验。只有单一赔率来源时也可以继续分析，但需要降低赔率置信度和参考等级，不能把单一投注平台当成市场共识。赔率、盘口、首发、伤停、天气等当前数据都应记录来源和观察时间。

明确不会做的事：

- 不登录投注平台账号。
- 不绕过验证码、地区限制、频率限制或付费访问限制。
- 不硬编码任何投注平台账号、数据源密钥或大模型 API key。
- 没有核验来源时，不声称拥有实时赔率。

更详细的数据源规则见 `references/data-sources.md`。

## 数据服务/API 怎么配置

这个 skill 不绑定某一个固定数据商，也不自带 API key。所谓“已配置的足球数据服务/API”，指的是运行这个 skill 的 agent 环境已经能调用某种数据能力，例如 MCP 工具、命令行工具、内部 HTTP 服务、本地 JSON/CSV 文件，或由用户授权的第三方足球数据 API。

## 推荐可选数据源

下面是可以考虑配置的第三方数据源。它们不是这个 skill 的内置依赖，也不是默认自动调用的数据源；使用前需要你自己注册账号、获取 API key，并确认额度、覆盖赛事、使用条款和地区限制。

- `API-Football`：偏足球全量数据，官方 pricing 页面显示 Free 计划为 `100 Requests / day`，并包含 Fixtures、Line Ups、Injuries、In-play Odds、Pre-match Odds 等能力。适合同时查赛程、首发伤停、球队数据和赔率。官方页面：https://www.api-football.com/pricing
- `The Odds API`：偏多博彩公司赔率聚合，官方首页显示 Starter 免费层为 `500 credits per month`，覆盖 soccer odds、head-to-head、spreads/handicap、totals/over-under 等市场。适合专门查多来源赔率和盘口。官方页面：https://the-odds-api.com/

免费额度和接口范围可能变化，实际可用能力以官方页面和你的账号权限为准。README 只说明推荐配置方向，不保证这些服务长期免费或覆盖你要分析的每场比赛。

推荐配置方式：

1. 在 agent 运行环境中配置数据源能力，例如 `API_FOOTBALL_KEY`、`THE_ODDS_API_KEY`、`WEATHER_API_KEY` 这类环境变量，或配置一个可调用的本地/内部数据服务。
2. 让 agent 的工具层暴露清晰的数据能力，例如“查赛程”“查赔率”“查首发伤停”“查天气”“查球队近期数据”。
3. 不要把 API key 写进 `README.md`、`SKILL.md`、`references/`、示例文件或报告正文。
4. 如果只是在聊天环境中使用，没有可调用 API，就把赔率、盘口、比赛时间、伤停和近期状态按“输入模板”提供给 agent。

`.env` 风格示例：

```bash
API_FOOTBALL_KEY=your_api_football_key
THE_ODDS_API_KEY=your_the_odds_api_key
WEATHER_API_KEY=your_weather_api_key
```

这个仓库当前提供的是 skill 规则、参考文档、schema、示例和计算脚本；没有实现具体数据商的 API 客户端。将来如果要接入某个数据商，应新增独立的工具或适配脚本，并继续遵守 `references/data-sources.md` 中的来源、时间戳、冲突处理和降级规则。

## 未配置 API 时的默认行为

未配置足球数据服务或博彩公司 API 时，默认不会调用某个“免费博彩公司 API”。原因是这个 skill 没有内置任何博彩公司接口，也不会假设某个接口在所有环境里可用、合规、稳定或免授权。大模型可以搜索“有哪些 API 或公开网页”，但“发现某个 API 存在”不等于“已经有权限调用它”。

默认行为是：

1. 如果 agent 有搜索、浏览器或网页读取工具，先主动核验公开/授权网页上的赛程、赔率、盘口、首发、伤停、天气和球队数据。
2. 如果公开网页能访问，就把公开来源和观察时间写入报告；如果只有一个赔率来源，则降低赔率置信度。
3. 如果公开网页不可访问、需要登录、被验证码拦截、来源冲突或 agent 没有联网能力，就要求用户提供最少必要数据。
4. 如果用户也无法提供赔率，skill 只能做概率分析，不能做赔率价值判断或“值得买”的判断。

也就是说，不配置 API 时的默认数据路径是“agent 主动公开核验，失败后用户补充”，不是“自动使用某个博彩公司免费 API”。

明确不会自动做的事：

- 不自动注册 API 账号。
- 不自动申请、猜测或生成 API key。
- 不自动调用未知免费 API。
- 不把公开网页上看到的接口当成已授权接口。
- 不绕过登录、验证码、地区限制、频率限制或付费墙。

## 公开网页核验由谁处理

如果 agent 当前具备浏览器、搜索或网页读取工具，公开网页核验应由 agent 主动完成：它先搜索或打开公开/授权页面，确认赛程、赔率、盘口、首发、伤停、天气和球队数据，再在报告里写明来源和观察时间。

用户不需要手动输入公开网页上的所有数据，除非出现下面情况：

- 当前 agent 没有联网、搜索或浏览器能力。
- 公开网页无法访问、需要登录、被验证码拦截或有地区/付费限制。
- 多个公开来源之间存在冲突，需要用户指定信任来源。
- 用户手里有更权威的数据，例如截图、表格、订阅数据、内部数据或指定链接。

如果公开核验失败，agent 应说明失败的类别，并只追问完成分析所需的最少字段，而不是自行猜测当前赔率或赛程。

## 数学计算引擎

这个 skill 使用的是透明的辅助决策模型，不是保证命中的预测引擎。核心计算流程是：

1. 估计主队和客队的预期进球，记作 `home_xg` 和 `away_xg`。
2. 根据伤停、首发、赛程密度、天气、战意和盘口变化做贝叶斯修正。
3. 用泊松分布生成比分矩阵，汇总出胜/平/负概率、比分候选和大小球概率。
4. 将十进制赔率转换为隐含概率，并在完整市场中计算去水概率。
5. 比较模型概率和市场去水概率，判断是否存在轻微、明确或不足的赔率价值。

内置脚本只负责确定性计算，不负责抓取外部数据：

- `scripts/poisson_calculator.py`：根据双方预期进球计算比分矩阵、胜平负概率、大小球概率和高概率比分候选。
- `scripts/implied_probability.py`：根据十进制赔率计算原始隐含概率、市场水位和去水概率。
- `scripts/validate_inputs.py`：校验示例或结构化输入是否符合 schema，并提示缺失来源、时间戳或数据冲突。

如果脚本不可用，agent 可以给出区间或近似值，但必须标注为近似，不能伪装成精确计算。更详细的模型说明见 `references/math-model.md`。

## 数据不足时如何处理

当没有赔率时，skill 只能做概率分析，不能做赔率价值判断；当赛程、主客/中立场、首发、伤停或盘口存在关键缺口时，应降低数据置信度、参考等级，必要时给出 Pass。

当没有联网、浏览器、API 或本地数据时，skill 应进入离线 fallback：说明缺少哪些当前数据，要求用户补充最少信息，并避免从模型记忆中编造当前赛程、赔率、伤停或天气。

## 不做什么

- 不保证赛果。
- 不提供投注金额、资金分配或 Kelly 仓位建议。
- 不提供追损、回本或翻本建议。
- 不登录投注平台账号，不绕过访问限制。
- 不硬编码足球数据凭据或大模型 API key。

## 示例提问

```text
帮我分析今晚西班牙 vs 沙特，给胜平负、让球、大小球和三个比分候选。
```

```text
明天早上四场世界杯比赛，帮我做一个四串一参考购买方案，重点看比分和大小球。
```

```text
现在不能联网。我给你赔率和球队近况，你按离线模式分析法国 vs 日本有没有价值。
```

```text
A站说德国让1球，B站说德国让1.25；A站说主力前锋伤缺，B站没提。帮我分析风险。
```

```text
复盘一下我昨天买的四串一为什么没中，重点看模型偏差和数据偏差。
```

## 带盘口赔率的使用例子

如果你已经有赔率和盘口，可以直接贴给 agent。示例：

```text
帮我分析西班牙 vs 沙特。

比赛时间：2026-06-23 08:00 CST
赛事：世界杯小组赛
场地：中立场

胜平负赔率：
主胜 1.52，平 4.10，客胜 7.20

让球胜平负：
西班牙 -1，胜 2.28，平 3.35，负 2.55

大小球：
2.5 球，大 1.95，小 1.85

伤停/首发：
西班牙主力前锋预计首发，沙特中卫一人伤疑；首发未确认。

近期状态：
西班牙近 10 场进攻稳定；沙特面对强队时防线压力较大。

请给胜平负、让球、大小球、三个比分候选和风险点。
```

如果你只想让 agent 自己查公开数据，可以这样问：

```text
帮我查一下今晚西班牙 vs 沙特的公开赔率和盘口，再做胜平负、大小球和比分分析。只使用公开或授权来源，查不到就告诉我缺什么。
```

## 输入模板

当 agent 无法核验当前数据时，可以按下面模板补充信息：

```markdown
比赛：
开赛时间：
赛事：
主客/中立：
想看玩法：胜平负 / 让球胜平负 / 比分 / 大小球 / 四串一
赔率/盘口：
伤停/首发：
近期状态或数据：
```

## 输出内容

单场报告应包含：

- 数据确认：赛程、赔率/盘口、近期状态、xG 或进失球数据、伤停、首发、天气、战意和数据缺口。
- 结论摘要：胜平负、让球、大小球、参考等级、模型置信度、数据置信度。
- 概率判断：胜平负概率和大小球概率。
- 模型依据：预期进球、泊松比分集中、贝叶斯修正，以及有赔率时的赔率价值对比。
- 三个比分候选：主推、次选和冷门/风险比分。
- 参考购买方案：非保证性的参考方案，以及不建议纳入的项目。
- 风险点：临场首发、单源赔率、数据冲突、市场一致性或其他不确定性。

组合报告应包含比赛确认、每场简版结论、组合相关性检查、主组合/保守替代/激进替代，以及 Pass 或排除项说明。

赛后复盘应对比赛前假设和实际结果，识别模型偏差和数据偏差，并避免任何追损建议。

## 开发检查

运行脚本单测：

```bash
python3 football-betting-assistant/tests/test_scripts.py
```

校验示例输入：

```bash
python3 football-betting-assistant/scripts/validate_inputs.py football-betting-assistant/examples/single-match-input.json
python3 football-betting-assistant/scripts/validate_inputs.py football-betting-assistant/examples/portfolio-input.json
```

运行验收检查：

```bash
python3 football-betting-assistant/scripts/run_acceptance_checks.py
```
